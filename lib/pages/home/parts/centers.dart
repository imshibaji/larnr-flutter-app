import 'package:flutter/material.dart';

class CentersPart extends StatelessWidget {
  const CentersPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Centers Section'));
  }
}
