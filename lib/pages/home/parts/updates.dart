import 'package:flutter/material.dart';

class UpdatesPart extends StatelessWidget {
  const UpdatesPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Updates'));
  }
}
