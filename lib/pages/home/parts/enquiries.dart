import 'package:flutter/material.dart';

class EnquiriesPart extends StatelessWidget {
  const EnquiriesPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Enquiries Section'));
  }
}
