import 'package:flutter/material.dart';

class CoursesPart extends StatelessWidget {
  const CoursesPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Courses Section'));
  }
}
