import 'package:flutter/material.dart';

class CallsPart extends StatelessWidget {
  const CallsPart({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Calls'));
  }
}
